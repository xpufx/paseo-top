{
  "name": "top",
  "private": true,
  "version": "0.3.0",
  "scripts": {
    "build": "node -e \"import('paseo-plugin-helper/server').then(m => m.stampVersion({ targetFile: 'shared/version.ts' }))\"",
    "pretypecheck": "npm run build",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npx tsx --test server/telemetry.test.ts client/pill-labels.test.ts"
  },
  "devDependencies": {
    "@getpaseo/plugin": "0.8.0-beta.1",
    "@tanstack/react-query": "^5.90.11",
    "@types/react": "~19.2.0",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  },
  "dependencies": {
    "paseo-plugin-helper": "^0.4.0-beta.7"
  }
}
